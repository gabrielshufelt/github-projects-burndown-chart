query OrganizationProject($organization_name: String!, $project_number: Int!, $labels_per_issue_count: Int!, $cursor: String, $iteration_field_name: String = "Iteration") {
  organization(login: $organization_name) {
    projectV2(number: $project_number) {
      title
      field(name: "Status") {
        ... on ProjectV2SingleSelectField {
          options {
            name
          }
        }
      }
      iterationField: field(name: $iteration_field_name) {
        ... on ProjectV2IterationField {
          configuration {
            iterations {
              id
              title
              startDate
              duration
            }
          }
        }
      }
      items(first: 100, after: $cursor) {
        pageInfo {
            hasNextPage
            endCursor
        }
        nodes {
          id
          fieldValueByName(name: "Status") {
            ... on ProjectV2ItemFieldSingleSelectValue {
              name
            }
          }
          iterationValue: fieldValueByName(name: $iteration_field_name) {
            ... on ProjectV2ItemFieldIterationValue {
              iterationId
            }
          }
          content {
            ...on Issue {
              title
              timelineItems(first: 20, itemTypes: [ASSIGNED_EVENT]) {
                nodes {
                  __typename
                  ... on AssignedEvent {
                    createdAt
                  }
                }
              }
              createdAt
              closedAt
              labels(first: $labels_per_issue_count) {
                nodes {
                  name
                }
              }
            }
            ...on PullRequest {
              title
              timelineItems(first: 20, itemTypes: [ASSIGNED_EVENT]) {
                nodes {
                  __typename
                  ... on AssignedEvent {
                    createdAt
                  }
                }
              }
              createdAt
              closedAt
              labels(first: $labels_per_issue_count) {
                nodes {
                  name
                }
              }
            }
          }
        }
      }
    }
  }
}